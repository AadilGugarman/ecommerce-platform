// features/auth/components/AuthFormInput.jsx

import { TextField } from "@mui/material";

const AuthFormInput = (props) => {
  return <TextField fullWidth {...props} />;
};

export default AuthFormInput;
